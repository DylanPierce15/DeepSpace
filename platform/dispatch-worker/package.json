{
  "name": "@deepspace/dispatch-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:dry-run": "wrangler deploy --dry-run",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist *.tsbuildinfo .wrangler"
  },
  "dependencies": {
    "@cloudflare/puppeteer": "0.0.15",
    "deepspace": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241205.0",
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0",
    "wrangler": "^4.84.1"
  }
}
