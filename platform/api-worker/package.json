{
  "name": "@deepspace/api-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "generate:registry": "tsx scripts/generate-registry.ts",
    "predev": "tsx scripts/generate-registry.ts",
    "dev": "wrangler dev",
    "predeploy": "pnpm generate:registry",
    "deploy": "wrangler deploy",
    "type-check": "tsc --noEmit",
    "pretest": "pnpm generate:registry",
    "test": "vitest run",
    "test:watch": "vitest",
    "clean": "rm -rf dist *.tsbuildinfo .wrangler"
  },
  "dependencies": {
    "deepspace": "workspace:*",
    "drizzle-orm": "^0.38.0",
    "gtfs-realtime-bindings": "^1.1.1",
    "hono": "^4.6.0",
    "stripe": "^21.0.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.14.0",
    "@cloudflare/workers-types": "^4.20241205.0",
    "@types/node": "^22.10.0",
    "tsx": "^4.21.0",
    "typescript": "^5.7.0",
    "vitest": "^4.1.0",
    "wrangler": "^4.79.0"
  }
}
