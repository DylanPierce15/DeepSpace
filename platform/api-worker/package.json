{
  "name": "@deepspace/api-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "type-check": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "clean": "rm -rf dist *.tsbuildinfo .wrangler"
  },
  "dependencies": {
    "deepspace": "workspace:*",
    "drizzle-orm": "^0.38.0",
    "hono": "^4.6.0",
    "stripe": "^21.0.0"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.14.0",
    "@cloudflare/workers-types": "^4.20241205.0",
    "@types/node": "^22.10.0",
    "drizzle-kit": "^0.30.0",
    "typescript": "^5.7.0",
    "vitest": "^4.1.0",
    "wrangler": "^3.100.0"
  }
}
