{
  "name": "@deepspace/deploy-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist *.tsbuildinfo .wrangler"
  },
  "dependencies": {
    "@deepspace/auth": "workspace:*",
    "@deepspace/types": "workspace:*",
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241205.0",
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0",
    "wrangler": "^3.100.0"
  }
}
