# E2E test app — deployed to spaces-apps dispatch namespace
# Accessible at https://deepspace-sdk-test.app.space
# NOTE: Deploy/delete via Cloudflare REST API, not wrangler CLI

name = "deepspace-sdk-test"
main = "worker.ts"
compatibility_date = "2025-01-01"
compatibility_flags = ["nodejs_compat"]
